400_1111001_wrong_credentials:
  title: Неправильные учетные данные
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Wrong credentials"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1111001

400_1001001_wrong_user_data:
  title: Недопустимые пользовательские данные
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Wrong user data"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1001001

400_1001002_password_changing_error:
  title: Недопустимые пользовательские данные
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Password changing error"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1001002


404_2001001_user_not_found:
  title: Пользователь не найден
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "User not found"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 2001001

404_2002001_folder_not_found:
  title: Каталог не найден
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Folder not found"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 2002001


404_2003001_note_not_found:
  title: Заметки не найдена
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Note not found"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 2003001

400_1111002_file_is_missing:
  title: Файл не передан
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "File is missing"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1111002


422_base:
  title: Ошибка валидации
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        some_entry_name:
          type: array
          items:
            type: string
            example: "Some validation error."

400_1002001_folder_creation_error:
  title: Ошибка создания каталога
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Folder creation error"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1002001

400_1002002_folder_update_error:
  title: Ошибка обновления каталога
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Folder update error"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1002002

400_1003001_note_creation_error:
  title: Ошибка создания заметки
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Note creation error"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1003001

400_1003002_note_update_error:
  title: Ошибка обновления заметки
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "Note update error"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1003002

400_1003003_note_relation_creation_error:
  title: Ошибка создания связи между заметками
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "NoteRelation creation error"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1003003

400_1003004_note_relation_delete_error:
  title: Ошибка удаления связи между заметками
  type: object
  properties:
    error:
      type: object
      description: Дополнительные поля описывающие причину ошибки
      properties:
        message:
          type: string
          description: Описание ошибки
          example: "NoteRelation delete error"
        code:
          type: integer
          description: Внутренний код ошибки
          example: 1003004
